<Window x:Class="BeautySalon17.Windows.OrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BeautySalon17.Windows" xmlns:system="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="OrderWindow" Height="450" Width="800">
   
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Выбор даты получения (не более 7 дней от текущей) -->
        <TextBlock Grid.Row="0" Text="Выберите дату получения:" FontWeight="Bold" Margin="0,0,0,5"/>
        <DatePicker x:Name="DpPickupDate" Grid.Row="1" 
            SelectedDate="{x:Static system:DateTime.Today}"
            Margin="0,0,0,15"/>

        <!-- Выбор способа оплаты -->
        <TextBlock Grid.Row="2" Text="Способ оплаты:" FontWeight="Bold" Margin="0,0,0,5"/>
        <StackPanel Grid.Row="3">
            <RadioButton x:Name="RbCash" Content="Наличные" IsChecked="True" Margin="0,0,0,5"/>
            <RadioButton x:Name="RbCard" Content="Банковская карта"/>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Отмена" Width="80" Height="30" IsCancel="True" Click="BtnCancel_Click"/>
            <Button Content="Подтвердить" Width="100" Height="30" Margin="10,0,0,0" Click="BtnConfirm_Click"/>
        </StackPanel>
    </Grid>
</Window>

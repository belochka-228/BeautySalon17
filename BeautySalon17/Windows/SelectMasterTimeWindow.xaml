<Window x:Class="BeautySalon17.Windows.SelectMasterTimeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BeautySalon17.Windows"
        mc:Ignorable="d"
        Title="SelectMasterTimeWindow" Height="450" Width="800">
 
        <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Выбор мастера -->
        <TextBlock Grid.Row="0" Text="Выберите мастера:" FontWeight="Bold" Margin="0,0,0,5"/>
        <ComboBox x:Name="CmbMaster" Grid.Row="1" Height="30" Margin="0,0,0,15"
                  DisplayMemberPath="FullName" SelectedValuePath="Id"/>

        <!-- Выбор времени -->
        <TextBlock Grid.Row="2" Text="Выберите время:" FontWeight="Bold" Margin="0,0,0,5"/>
        <ComboBox x:Name="CmbTime" Grid.Row="3" Height="30" VerticalAlignment="Top"
                  DisplayMemberPath="TimeSlot" SelectedValuePath="TimeSlot"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Отмена" Width="80" Height="30" IsCancel="True" Click="BtnCancel_Click"/>
            <Button Content="Далее" Width="100" Height="30" Margin="10,0,0,0" Click="BtnNext_Click"/>
        </StackPanel>
    </Grid>
</Window>